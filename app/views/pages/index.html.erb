<h1>My rails app</h1>

<nav>
  <ul>
    <% @navigation_items.each do |navigation_item| %>
      <li><%= link_to navigation_title(navigation_item), navigation_link(navigation_item) %></li>
    <% end %>
  </ul>
</nav>
